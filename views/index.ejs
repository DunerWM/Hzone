<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='build/stylesheets/style.css'/>
    <link rel='stylesheet' href='build/stylesheets/reset.css'/>
    <link rel='stylesheet' href='build/stylesheets/public.css'/>
    <script src="build/javascripts/angular.min.js"></script>
</head>
<body ng-app="myapp" ng-controller="mainCtrl"  ng-click="hideLoginRegister($event)">
<div class="main-container">
    <div class="banner">
        <div class="banner-inner">
            <h1>Hzone</h1>
            <p class="sub-title">蹲在屋檐下看蚂蚁搬家</p>
        </div>
    </div>
    <div class="content-container">
        <div class="content-container-right">
            <div class="user-section">
                <% if (user) { %>
                <a href="#" title="<%=user.name%>"><%=user.name%></a>
                <a href="#" title="登出">登出</a>
                <% } %>
            </div>
        </div>
        <div class="content-container-left">
            <% if (user) {%>
            <div class="post-area" ng-controller="stateFormCtrl" ng-init="tabId = 'null'">
                <p><img src="build/images/font.png"><span>还可以输入200字</span></p>
                <form name="postStateForm">
                    <textarea id="postStates" cols="50" rows="3" class="post-states" ng-model="state.content" name="state.content"></textarea>
                </form>
                <div class="tools-bar">
                    <a href="###" class="tool-emotion" ng-click="changeTabIdEmotion()">表情</a>
                    <a href="###" class="tool-image" ng-click="changeTabIdPhoto()">照片</a>

                    <a href="###" class="tool-article" ng-click="changeTabIdBlog()">博客</a>
                    <a class="state-submit" ng-click="stateSubmit()">发布</a>
                </div>
                <div ng-if="tabId == 'emotion'">
                    <div class="state-frame emotions-frame" state-emotions></div>
                </div>
                <div ng-if="tabId == 'photo'">
                    <div class="state-frame photos-frame" state-photos></div>
                </div>
                <div ng-if="tabId == 'blog'">
                    <div class="state-frame blog-frame" state-blog></div>
                </div>
            </div>
            <% } else { %>
            <div class="login-regester" ng-controller="loginRegisterCtrl">
                <a title="登录" ng-click="showLogin($event)">登录</a>后就可以发状态啦~ 我尚需
                <a href="#" title="注册" ng-click="showRegister($event)">注册1</a>
                <div class="login-register-container" ng-keyup="escClose($event)" ng-click="stopProp($event)">
                    <div ng-if="document.showLoginRegister == 'login'" ng-cloak focus-me tabindex="0">
                        <login class="login-container"></login>
                    </div>
                    <div ng-if="document.showLoginRegister == 'register'" ng-cloak focus-me tabindex="0">
                        <reigster class="register-container"></reigster>
                    </div>
                </div>
            </div>
            <% } %>
            <ul class="states-list">
                <% for(var i = 0; i < states.length; i++) { %>
                <li>
                    <div class="head-img">
                        <img src="/build/images/head_default.jpg">
                    </div>
                    <div class="state-details">
                        <p class="blog-title">
                            <a href="###"><%= states[i].doc[0].nick %></a>
                            <span>&nbsp;发布&nbsp;</span>
                            <%= states[i].blog.content %>
                        </p>
                        <div class="inner-content overf-h">
                            <img src="build/images/content_img_default.jpg" class="content-img">
                            <p><%= states[i].blog.content %></p>
                        </div>
                        <div class="state-bar">
                            <span class="tips">摄影 文艺 生活</span>
                            <a href="###">评论</a>
                            <a href="###">分享</a>
                            <a href="###">赞</a>
                        </div>
                    </div>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</div>
<script src="build/javascripts/emojify.min.js"></script>
<script src="build/javascripts/index.js"></script>
</body>
</html>
